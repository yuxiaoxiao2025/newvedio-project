使用“Chrome工具”访问链接： https://help.aliyun.com/zh/model-studio/text-to-speech?spm=a2c4g.11186623.help-menu-2400256.d_0_3_0.3eb611f067JKsC&scm=20140722.H_2842586._.OR_help-T_cn~zh-V_1 按照以下规范执行页面内容抓取、处理、存储与交付：
1. 页面内容抓取要求（确保全量覆盖，通用适配任意主题）
核心范围：精确提取左侧导航栏「语音合成」（自动识别当前访问页面左侧导航栏的核心分类，如本次“语音合成”，其他场景自动匹配对应分类名称）下的 全部子项及分级子页面，需覆盖以下完整链路：

一级子项（直接抓取）：自动识别「目标分类」下所有一级子项（如本次“语音合成”对应的一级子项，无需手动罗列，按页面实际导航栏内容全量抓取）

分级子项（含独立页面，需完整抓取所有下级内容）：自动识别「目标分类」下所有带下级页面的子项（如“工具调用”“专项模型”等，按页面实际层级全量抓取其下所有子页面）

内容完整性：每个网页需完整获取 全部核心文档内容（含正文段落、API参数表、代码示例、表格数据、列表信息、注释说明等），禁止遗漏或截断

开发语言规则：仅保留并提取 Python（首字母大写统一）和 Node.js 相关内容（含代码块、语法说明、参数配置、使用教程等关联信息），其他语言内容直接删除

2. 内容处理规范（解决格式矛盾，保留有效信息）
定位规则：使用XPath/CSS选择器精准定位【网页核心内容区域】（正文主体区、代码展示区、数据表格区、有序/无序列表区），明确排除页眉页脚、导航菜单、侧边栏等非核心区域

清除无关元素：

非必要HTML标签及属性（保留与格式相关的关键标签/属性：H1-H6、代码块标记、表格标签、列表标签，避免格式丢失）

广告位、推荐内容、用户评论、弹窗、版权信息栏、无关悬浮窗

页眉页脚、导航菜单（含左侧导航栏非目标子项）、侧边栏

保留关键格式：

标题层级：H1-H6保持原结构和层级关系，不篡改顺序

代码块：完整保留Python/Node.js代码的缩进、语法高亮标记、注释；代码块开头必须添加语言标识符（Python用 python，Node.js用 javascript）

表格数据：完整转换为Markdown表格语法，表头、表体数据无遗漏，对齐格式统一

有序/无序列表：保持原结构、层级和内容，不丢失列表项

文本净化处理：

删除无意义特殊符号（®、©、™等非内容类符号；保留与内容相关的符号，如括号、标点、公式符号）

修正乱码字符（如�符号）

保持中英文混排原貌及文本顺序，不删减任何核心语句

3. 文件存储与自动存放规则（统一规范，通用适配所有分类）
3.1 参考基准目录架构（通用模板，自动匹配当前分类）
documentation/
└── qwen/
├── 【当前分类名称】/ （自动识别当前抓取的「目标分类」名称，如本次为“语音合成”，其他场景自动替换为对应分类名）
│   ├── 【一级子项1】.md （自动匹配「目标分类」下一级子项名称，按实际内容生成）
│   ├── 【一级子项2】.md （示例：如“语音合成模型概述.md”“实时语音合成-CosyVoice.md”等）
│   └── 【分级子项文件夹1】/ （自动识别带下级页面的子项名称，如“工具调用”“专项模型”等）
│       ├── 【分级子项1】.md （按实际子页面名称生成，如“联网搜索.md”“长上下文（Qwen-Long）.md”）
│       └── 【分级子项2】.md
└── 【当前分类名称】/【分级子项文件夹2】/ （如“语音合成/专项模型/”，自动按实际层级生成）
├── 【分级子项3】.md
└── 【分级子项4】.md
3.2 存放要求（自动执行，无需手动干预）
目录创建：自动匹配基准目录的通用逻辑，在documentation/qwen/下按“【当前分类名称】/一级子项.md”“【当前分类名称】/分级子项文件夹/分级子项.md”的路径存放，无需手动创建文件夹，分类名称、子项名称均自动识别页面内容生成

文件名规范：统一为「子项名称.md」（与页面子项标题/实际内容完全一致，禁止自定义命名，如“实时语音合成-通义千问.md”“Function Calling.md”）

文件拆分规则：单个Markdown文件内容不超过3万字（约50页）；若单页面内容超标，按逻辑章节拆分，文件名标注序号（如“实时语音合成-1.md”“工具调用-2.md”），并在文件头部元信息添加“related: 【对应子项名称】（拆分文档）”说明

3.3 文件格式规范
头部元信息（自动填充，无需手动修改）：

title: 自动识别当前抓取子项名称（与子项页面标题 / 实际内容完全一致）update: 自动获取抓取处理当天日期（严格遵循 YYYY-MM-DD 格式）source: 自动提取当前抓取网页的原始 URL（对应子项 / 分级子页面的独立访问链接）

plaintext
语法要求：使用标准Markdown语法，通过Markdown lint工具校验无语法错误

4. 完整性与一致性验证流程（必做，避免遗漏/错误）
4.1 内容交叉检查
覆盖校验：确认「目标分类」下所有一级子项、分级子页面均已抓取，无遗漏

细节校验：验证每个API的参数表（字段、说明、类型、默认值）完整无缺；Python/Node.js代码示例无语法错误、可执行性正常

冗余校验：无广告、无关推荐等冗余内容残留

4.2 链接处理检查
内部链接：文档内跳转链接转换为基于documentation/qwen目录的相对路径（如“../专项模型/长上下文（Qwen-Long）.md”“../实时语音合成-CosyVoice.md”）

外部链接：统一格式为 [链接文本](链接地址) [来源] （如 [阿里云模型平台](https://help.aliyu  n.com/) [来源] ）

4.3 格式合规检查
代码块：所有Python/Node.js代码块均添加对应语言标识符，无裸代码块（即```后必须跟python/javascript）

表格：所有HTML表格已完整转换为Markdown表格，无格式错乱

标题：H1-H6层级清晰，不越级、不重复

5. 最终交付物要求
输出目录：严格输出到/documentation/qwen目录，目录结构与基准架构通用逻辑完全一致（无多余文件夹/文件，无缺失文件）

交付标准：所有Markdown文件符合上述存储、格式规范